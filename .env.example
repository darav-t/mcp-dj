# =============================================================================
# MCP DJ — Environment Configuration
# Copy this file to .env and fill in your values.
# Never commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Anthropic / Claude API
# Required for AI-powered chat and setlist generation.
# Get your key at https://console.anthropic.com/
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# Web server
# Port for the FastAPI web UI (default: 8888)
# -----------------------------------------------------------------------------
SETLIST_PORT=8888

# -----------------------------------------------------------------------------
# Mixed In Key CSV export (optional)
# Path to your Mixed In Key "Library.csv" export file.
# If not set, energy levels are inferred from BPM and genre instead.
# Default (macOS): ~/Music/Mixed In Key Data/Library.csv
# -----------------------------------------------------------------------------
# MIK_CSV_PATH=/path/to/Mixed In Key Data/Library.csv

# -----------------------------------------------------------------------------
# Rekordbox database (optional override)
# pyrekordbox auto-detects the Pioneer directory on macOS and Windows.
# Set this only if your Rekordbox library is in a non-standard location.
# macOS default: ~/Library/Pioneer
# Windows default: %APPDATA%/Pioneer
# -----------------------------------------------------------------------------
# REKORDBOX_DB_PATH=/path/to/Pioneer
